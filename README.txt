** INSTRUCTIONS **

-> Program/code runs in python 3.8.1
-> To install requirements use command "pip install -r requirements.txt"
-> To run use command "python main.py".
-> Algorithm is trained using "training.csv".
-> "predicted.csv" gives the predicted classes for the "testing.csv" file.

** Warning message shows up when running the code. It does not affect the execution.